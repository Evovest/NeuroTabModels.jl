import{_ as a,o as s,c as n,ao as t}from"./chunks/framework.BWJmd-WM.js";const u=JSON.parse('{"title":"Grouped padding and masks","description":"","frontmatter":{},"headers":[],"relativePath":"models/padding-mask.md","filePath":"models/padding-mask.md","lastUpdated":null}'),o={name:"models/padding-mask.md"};function d(p,e,i,l,r,c){return s(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="Grouped-padding-and-masks" tabindex="-1">Grouped padding and masks <a class="header-anchor" href="#Grouped-padding-and-masks" aria-label="Permalink to &quot;Grouped padding and masks {#Grouped-padding-and-masks}&quot;">​</a></h1><p><code>MLPAttn</code> and <code>NeuroTreeAttn</code> let information travel across observations that belong to the same group. A grouped loader (<code>group_name</code>) uses <code>batchsize=0</code>: each step is one group, padded to a common width. Attention’s own batch dimension is therefore always <code>1</code> — the sequence is the group.</p><p>This page covers that padding: where <code>w</code> comes from, why embeddings never see it, and how the <code>*Attn</code> core consumes it.</p><h2 id="Why-there-is-a-pad-at-all" tabindex="-1">Why there is a pad at all <a class="header-anchor" href="#Why-there-is-a-pad-at-all" aria-label="Permalink to &quot;Why there is a pad at all {#Why-there-is-a-pad-at-all}&quot;">​</a></h2><p>A grouped train/eval/infer loader takes <code>groupby(df, group_name)</code> and writes <strong>one padded tensor per group</strong>. The buffer width is the size of the largest group, so every group is a dense <code>(features, buffer)</code> matrix:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>one group, 3 real rows, buffer = 5</span></span>
<span class="line"><span></span></span>
<span class="line"><span>        col:   1    2    3    4    5</span></span>
<span class="line"><span>               ├──── real ────┤  pads</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   x  =  [  x₁   x₂   x₃   0    0  ]     (nfeats × buffer)</span></span>
<span class="line"><span>   y  =  [  y₁   y₂   y₃   0    0  ]</span></span>
<span class="line"><span>   w  =  [  1    1    1    0    0  ]     1 = real row, 0 = pad slot</span></span></code></pre></div><p>Train/eval use <code>w</code> as that 0/1 flag (shape <code>(1, 1, buffer)</code>). Infer uses a boolean <code>mask</code> of length <code>buffer</code> and, after the forward, keeps only <code>pred[:, mask]</code>.</p><p>Pads exist so the group is a rectangular array. They are not observations. Anything that reduces across the last dimension must ignore them. Per-column maps (embeddings, Dense heads) can run on pad columns without contaminating real ones.</p><h2 id="Two-jobs-for-w" tabindex="-1">Two jobs for <code>w</code> <a class="header-anchor" href="#Two-jobs-for-w" aria-label="Permalink to &quot;Two jobs for \`w\` {#Two-jobs-for-w}&quot;">​</a></h2><p><code>w</code> is reused for two different things, at two different places:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Job</th><th style="text-align:right;">Who uses it</th><th style="text-align:right;">What happens to pads</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Loss / metric weight</strong></td><td style="text-align:right;"><code>_reduce(loss, w)</code> and eval</td><td style="text-align:right;">pad slots contribute <code>0</code> to the scalar loss</td></tr><tr><td style="text-align:right;"><strong>Attention mask</strong></td><td style="text-align:right;"><code>MLPAttn</code> / <code>NeuroTreeAttn</code></td><td style="text-align:right;">pads are excluded from attention keys and from <code>MaskedBatchNorm</code> stats</td></tr></tbody></table><p>A non-attention model (<code>ResNet</code>, <code>MLP</code>, …) only needs the first job. Its forward sees <code>x</code> only; pad rows still produce a prediction, but that prediction is multiplied by <code>w = 0</code> in the loss. Grouped training works for those models without threading a mask through the net.</p><p>An <code>*Attn</code> model also needs the second job. A pad token with value <code>0</code> would still be a key every query can attend to. Encoder BatchNorm is a reduction over the group: pad zeros would enter <code>μ</code> / <code>σ</code> and running stats. Those layers must see a valid-token flag.</p><h2 id="Why-embeddings-do-not-take-w" tabindex="-1">Why embeddings do not take <code>w</code> <a class="header-anchor" href="#Why-embeddings-do-not-take-w" aria-label="Permalink to &quot;Why embeddings do not take \`w\` {#Why-embeddings-do-not-take-w}&quot;">​</a></h2><p>Embeddings are per-observation maps. A linear / periodic / piecewise-linear / identity / layer-norm embedding applied to column <code>j</code> does not read column <code>k</code>. Computing <code>embed(x_pad)</code> cannot change <code>embed(x_real)</code>.</p><p>The mask is therefore not an embedding concern. The split is implemented once, at the embed / core boundary:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>input is either x  or  (x, w)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>                    (x, w)</span></span>
<span class="line"><span>                       │</span></span>
<span class="line"><span>          ┌────────────┴────────────┐</span></span>
<span class="line"><span>          │      MaskedModel        │</span></span>
<span class="line"><span>          │                         │</span></span>
<span class="line"><span>          │   z = embed(x)          │  ← embeddings: x only</span></span>
<span class="line"><span>          │                         │</span></span>
<span class="line"><span>          │   y = core((z, w))      │  ← *Attn / MaskedBN: (z, w)</span></span>
<span class="line"><span>          └────────────┬────────────┘</span></span>
<span class="line"><span>                       │</span></span>
<span class="line"><span>                       y</span></span></code></pre></div><p><code>fit</code> chooses that wrapper from the core, not from the loader:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">chain </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> uses_batch_mask</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(core_chain)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    MaskedModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(embed_chain, core_chain)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># MLPAttn, NeuroTreeAttn</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    Chain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(embed_chain, core_chain)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># everyone else</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>Chain(embed, core)</code> would pass <code>(x, w)</code> into <code>embed</code>, which does not implement that signature. <code>MaskedModel</code> peels the tuple: embeddings stay ordinary Lux layers; only the core is mask-aware.</p><p>Call sites do not special-case embeddings either. <code>masked_input</code> decides whether <code>w</code> is forwarded into the assembled chain:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Chain:         masked_input(model, x, w) = x</span></span>
<span class="line"><span>MaskedModel:   masked_input(model, x, w) = (x, w)</span></span></code></pre></div><p>Loss, eval, and infer all go through that hook. The loader always produces <code>(x, y, w)</code> for grouped data; only mask-aware chains unpack <code>w</code> as a model input.</p><p>Pad columns are still forwarded. After <code>embed(x)</code> the core keeps a rectangular <code>(hidden, buffer)</code> stream: attention and <code>MaskedBatchNorm</code> drop pads from reductions, the head still emits a prediction per slot, and the loss zeros those slots. Embeddings may produce garbage on pad columns; those tokens must not leak into real tokens downstream.</p><p><code>BatchNormEmbeddings</code> is the exception: it does reduce across the group, and today it does not see <code>w</code>. Prefer identity / linear / LN embeddings (or the model’s own <code>MaskedBatchNorm</code>) when training an <code>*Attn</code> model on grouped data.</p><h2 id="End-to-end-flow" tabindex="-1">End-to-end flow <a class="header-anchor" href="#End-to-end-flow" aria-label="Permalink to &quot;End-to-end flow {#End-to-end-flow}&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GroupedDataFrame</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  padded buffers          x  (nfeats × buffer)</span></span>
<span class="line"><span>                        w/mask  (1 on real, 0/false on pad)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  masked_input(chain, x, w)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├── Chain:          x                 → embed(x) → core(z)</span></span>
<span class="line"><span>        └── MaskedModel:    (x, w)            → embed(x) → core(z, w)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ▼</span></span>
<span class="line"><span>  y  (outsize × buffer)     pad slots still have a number</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>        ├── train/eval:  loss weighted by w  (pads drop out)</span></span>
<span class="line"><span>        └── infer:       pred[:, mask]       (pads dropped after forward)</span></span></code></pre></div><h2 id="Inside-MLPAttn-/-NeuroTreeAttn" tabindex="-1">Inside MLPAttn / NeuroTreeAttn <a class="header-anchor" href="#Inside-MLPAttn-/-NeuroTreeAttn" aria-label="Permalink to &quot;Inside MLPAttn / NeuroTreeAttn {#Inside-MLPAttn-/-NeuroTreeAttn}&quot;">​</a></h2><p><code>MLPAttn</code> converts <code>w</code> to a boolean <code>valid</code> once, then uses the same flags in two shapes.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>(x, w)</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ├─ valid = vec(w) .&gt; 0          length = buffer</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ▼</span></span>
<span class="line"><span>encoder  ←  (x, valid)</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   │   residual MLP, same recipe as ResNet, but BN is masked:</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   │   CarryMask(Dense)     (h, valid) → (Dense(h), valid)</span></span>
<span class="line"><span>   │   MaskedBatchNorm      μ, σ from columns where valid is true</span></span>
<span class="line"><span>   │   CarryMask(Dropout)</span></span>
<span class="line"><span>   │   MaskSkip             (h, valid) → (h + f(h), valid)</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ▼</span></span>
<span class="line"><span>z, drop the tuple</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ▼</span></span>
<span class="line"><span>AttnResidual  ←  (z, mask)</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   │   mask = reshape(valid, buffer, 1, 1, 1)</span></span>
<span class="line"><span>   │   Q = K = W_qk z ,   V = z</span></span>
<span class="line"><span>   │   attention batch dim = 1  (the group is the sequence)</span></span>
<span class="line"><span>   │   rectangular key-padding mask (not causal):</span></span>
<span class="line"><span>   │   padded keys are zeroed for every query</span></span>
<span class="line"><span>   │</span></span>
<span class="line"><span>   ▼</span></span>
<span class="line"><span>head(z)        Dense on every column, including pads</span></span></code></pre></div><p><code>CarryMask</code> / <code>MaskSkip</code> exist so a <code>Chain</code> of ordinary layers can thread <code>(features, mask)</code> without each <code>Dense</code> knowing about padding. <code>MaskedBatchNorm</code> is the layer that actually reads the flag.</p><p><code>NeuroTreeAttn</code> is the same split with a tree encoder: trees are per-observation (<code>CarryMask</code> around <code>NeuroTree</code> + flatten), then <code>MaskedBatchNorm</code> uses the valid-token flag like MLPAttn. Attention blocks receive a rectangular key-padding mask. The hidden width is NeuroTree <code>k</code> (one ensemble of <code>ntrees</code> per channel, scalar leaf preds), not the number of leaves.</p><p>The mask is a key-padding mask over observations in the group, not a feature mask and not a causal triangle.</p><h2 id="Ungrouped-batches" tabindex="-1">Ungrouped batches <a class="header-anchor" href="#Ungrouped-batches" aria-label="Permalink to &quot;Ungrouped batches {#Ungrouped-batches}&quot;">​</a></h2><p>With no <code>group_name</code>, there are no pad slots. Every column is a real observation. <code>*Attn</code> models still accept a plain <code>x</code> (the unmasked method). <code>n_attn_layers=0</code> on <code>MLPAttn</code> is that encoder plus a linear head: a ResNet ablation, including <code>MaskedBatchNorm</code> with <code>valid = nothing</code>, which is ordinary BatchNorm.</p><p>If an ungrouped loader also carries sample weights, <code>*Attn</code> chains receive <code>(x, w)</code> because <code>masked_input(::MaskedModel, x, w) = (x, w)</code>. Zero weights are then treated as padded keys. Keep sample weights strictly positive, or use grouped <code>w</code> only as a 0/1 pad flag (as the grouped loader does).</p>`,36)])])}const g=a(o,[["render",d]]);export{u as __pageData,g as default};
